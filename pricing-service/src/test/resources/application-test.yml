spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

pricing:
  warehouse:
    datasource:
      url: jdbc:h2:mem:warehouse_testdb
      driver-class-name: org.h2.Driver
      username: sa
      password:

logging:
  level:
    com.destore: DEBUG
